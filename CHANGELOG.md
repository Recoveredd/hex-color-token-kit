# Changelog

## 0.1.1

- Accept unknown runtime input in token parsing and extraction helpers without throwing.
- Add diagnostics for non-string source text and invalid `maxInputLength` options.

## 0.1.0

- Initial release with CSS hex color token validation, extraction, normalization, and diagnostics.
